export interface UserStateInterface {
  firstname?: string;
  lastname?: string;
  nickname?: string;
  email?: string;
}

const state = (): UserStateInterface => {
  return {
    
  };
}

export default state;
